import 'package:flutter/material.dart';

class OnboardingBodyBuildingStep extends StatelessWidget {
  const OnboardingBodyBuildingStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
